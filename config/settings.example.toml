# Example CQRP runtime settings. Copy values into local environment variables; never commit secrets.
environment = "development"
poll_interval_seconds = 3

[storage]
database_path = "data/cqrp.db"
log_dir = "logs"

[snapshot_capture]
# Equivalent environment variable: CQRP_SNAPSHOT_INTERVAL_SECONDS
interval_seconds = 3
