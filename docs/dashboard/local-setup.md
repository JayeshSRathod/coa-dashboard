# Local setup
Install dependencies with `python -m pip install -r requirements.txt`. Start locally with `streamlit run dashboard/app.py`. The dashboard starts with graceful empty states when providers are not configured. Never place tokens in the repository.
